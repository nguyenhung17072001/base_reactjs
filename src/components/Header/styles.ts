import { styled } from '@mui/system';

export const HomeStyles = styled('div')( {
    toolbar: {
      justifyContent: 'space-between',
      alignItems: 'center',
      width: '100%',
      paddingTop: '20px',
      paddingBottom: '20px',
    },
    text: {
      color: 'red'
    }
    
  });
